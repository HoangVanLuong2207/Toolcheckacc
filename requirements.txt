selenium>=4.0.0
webdriver-manager>=3.0.0
colorama>=0.4.6
flask>=2.0.1
python-dotenv>=0.19.0
gunicorn>=20.1.0

Quick setup on a fresh Windows machine:
1. Install Python 3.7+ and tick "Add Python to PATH" during setup.
2. Install all deps: pip install -r requirements.txt
3. Core checker only: pip install selenium webdriver-manager colorama
4. From the project folder run: python check_garena.py
